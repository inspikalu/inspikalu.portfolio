---
import { LuGithub } from "react-icons/lu";
import { FaXTwitter } from "react-icons/fa6";
import { FaLinkedinIn } from "react-icons/fa";

const githubUrl = "https://github.com/inspikalu";
const linkedInUrl = "https://ng.linkedin.com/in/inspikalu";
const xUrl = "https://x.com/inspikalu";
const iconSize = 20;

// Navigation links
const navLinks = [
  { name: "Home", href: "/" },
  { name: "About", href: "/#about" },
  { name: "Projects", href: "/projects" },
  { name: "Contact", href: "/#contact" },
];

const currentYear = new Date().getFullYear();
const email = "inspikalu@gmail.com";
---

<footer class="bg-background py-10 border-t border-primary/10">
  <div class="mx-auto w-[90%] text-text">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
      <!-- Brand / About -->
      <div class="flex flex-col gap-4">
        <div>
          <h2 class="text-xl font-bold text-primary">Okereke Clement</h2>
          <p class="text-sm mt-1">Frontend Developer</p>
        </div>
        <p class="text-text/70 text-sm">
          Creating clean, responsive, and user-friendly web experiences with
          modern technologies.
        </p>
        <div class="flex items-center gap-4 mt-2">
          <a
            href={githubUrl}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="GitHub Profile"
            class="text-text hover:text-primary transition-colors"
          >
            <LuGithub size={iconSize} />
          </a>
          <a
            href={xUrl}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="Twitter Profile"
            class="text-text hover:text-primary transition-colors"
          >
            <FaXTwitter size={iconSize} />
          </a>
          <a
            href={linkedInUrl}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="LinkedIn Profile"
            class="text-text hover:text-primary transition-colors"
          >
            <FaLinkedinIn size={iconSize} />
          </a>
        </div>
      </div>

      <!-- Quick Links -->
      <div class="flex flex-col gap-4">
        <h3 class="text-lg font-semibold">Quick Links</h3>
        <nav>
          <ul class="flex flex-col gap-2">
            {
              navLinks.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-text/80 hover:text-primary transition-colors flex items-center gap-2"
                  >
                    <span class="text-primary">→</span> {link.name}
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>
      </div>

      <!-- Contact Info -->
      <div class="flex flex-col gap-4">
        <h3 class="text-lg font-semibold">Get In Touch</h3>
        <p class="text-text/70">
          Interested in working together? Feel free to reach out.
        </p>
        <a
          href=`mailto:${email}`
          class="text-text/80 hover:text-primary transition-colors"
        >
          {email}
        </a>
        <a
          href="/#contact"
          class="inline-block bg-primary text-button-text px-4 py-2 rounded text-sm hover:bg-primary/90 transition-colors mt-2 w-fit"
        >
          Contact Me
        </a>
      </div>
    </div>

    <!-- Copyright -->
    <div
      class="pt-6 border-t border-primary/10 flex flex-col md:flex-row justify-between items-center gap-4"
    >
      <p class="text-text/60 text-sm">
        © {currentYear} Okereke Clement Kalu. All rights reserved.
      </p>
      <p class="text-text/60 text-sm">Designed and built with 💜</p>
    </div>
  </div>
</footer>
